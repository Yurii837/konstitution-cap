import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'konst96-ang',
  webDir: 'dist/konst96-ang',
  server: {
    androidScheme: 'https'
  }
};

export default config;
